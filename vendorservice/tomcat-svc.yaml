kind: Service
apiVersion: v1
metadata:
  name: vmtomcat
  namespace: builduser
spec:
  type: LoadBalancer
  selector:
    app: vmtomcat
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080

